# Data-First Football v1 Configuration

This directory owns configuration introduced by the data-first football
forecasting program. These configurations must write only beneath
`artifacts/research/data-first-football-v1/` and cannot reference production
output prefixes.

The directory is intentionally configuration-free in Phase 0. Data and model
configuration begins under the applicable later implementation contract.
