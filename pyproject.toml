[project]
name = "cks_picks_cfb"
version = "0.1.0"
description = "An end-to-end college football betting model that predicts point spreads and over/unders."
requires-python = ">=3.12,<3.13"
authors = [{ name = "Connor Kitchings", email = "connor.kitchings@gmail.com" }]
dependencies = [
    "pandas>=2.0",
    "scikit-learn>=1.4",
    "requests>=2.30",
    "python-dotenv>=1.0",
    "cfbd==5.20.1", # Generated client pinned to the audited CFBD API contract
    "pyarrow>=15.0", # Parquet IO
    "jinja2>=3.1", # Email template rendering (publish_review.py)
    "mlflow>=2.0",
    "hydra-core>=1.3.2",
    "hydra-optuna-sweeper==1.2.0",
    "optuna<3.0.0,>=2.10.0",
    "lightgbm>=4.5.0",
    "catboost>=1.2.7",
    "geopy>=2.4.1",
    "xgboost>=1.5.0",
    "tabulate>=0.9.0",
    "typer>=0.20.0",
    "rich>=13.9.4",
    "boto3>=1.35.0,<2.0.0", # AWS/R2 cloud storage; <2.0.0 guards against breaking SDK changes
    "psycopg[binary]>=3.2", # Neon Postgres writes from publish_to_db
]

[project.optional-dependencies]
dev = [
    "rich>=13.0",
    "ruff>=0.12.7",
    "ipykernel==6.29.5",
    "pytest>=8.4.1",
    "pandas-stubs==2.2.0.240218",
    "types-requests==2.31.0.20240218",
    "mkdocs-material==9.5.7",
    "mkdocs==1.5.3",
    "mike==2.0.0",
    "seaborn>=0.13.0",
    "matplotlib>=3.8.0",
    "typer>=0.9.0",
]
research = [
    # Research dashboards and local exploration — not needed for pipeline or web app
    "streamlit>=1.30",
    "plotly>=5.18.0",
    # Experimental Bayesian modeling
    "pymc>=5.0.0",
    # Feature attribution analysis
    "shap>=0.50.0",
    # Unused standalone API server
    "fastapi>=0.116.1",
    "uvicorn[standard]>=0.35.0",
]

[tool.ruff]
line-length = 88
extend-exclude = [
    "*.ipynb",
    "**/*.ipynb",
    "research/**",
    "archive/**",
    "scripts/archive/**",
]
force-exclude = true

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]
ignore = ["E501"]



[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[dependency-groups]
dev = [
    "mkdocs-material>=9.5.7",
    "mkdocstrings[python]>=0.28.3",
    "matplotlib>=3.8.0",
    "seaborn>=0.13.0",
    "typer>=0.9.0",
    "bandit>=1.9.3",
    "pytest>=8.4.1",
    "pytest-mock>=3.15.1",
]

[tool.pytest.ini_options]
pythonpath = [
  "src"
]
norecursedirs = [
  "research",
  "archive",
  "scripts/archive",
  "web",
  "artifacts",
  "contracts",
  "assets",
]
