# Deprecated migration location

`contracts/migrations/` is the sole append-only migration history. The SQL files in
this directory are retained only to identify the pre-modernization legacy baseline;
do not add or apply new migrations here.
